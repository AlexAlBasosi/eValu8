#ifndef MAX_H
#define MAX_H


class Max
{
    public:
        Max();
        float max(float, float);
        float max(float, float, float);
        float maxarr(float[], int);

    protected:

    private:
};

#endif // MAX_H
