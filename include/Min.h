#ifndef MIN_H
#define MIN_H


class Min
{
    public:
        Min();
        float min(float, float);
        float min(float, float, float);
        float min(float, float, float, float);
        float min(float, float, float, float, float);
        float minarr(float[], int);

    protected:

    private:
};

#endif // MIN_H
